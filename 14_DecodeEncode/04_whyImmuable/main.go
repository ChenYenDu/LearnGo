package main

func main() {
	/*
		In this leature, we will learn why string is read-only (immutable).

		"hello" -> read-only byte slice ['h', 'e', 'l', 'l', 'o']
			-> backing array (String Header)
			Pointer,
			Length: len("hello"),
	*/
}
